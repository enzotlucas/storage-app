@model EnterprisesPageViewModel;

@{
    ViewData["Title"] = "Empresas - Serviço de estoque e vendas";
}

<h1>Empresas</h1>

<p>
    <a asp-action="Create">Cadastrar nova empresa</a>
</p>

<section class="page-content">
    <table class="table">
        <thead>
            <tr>
                <th>
                    Nome
                </th>
                <th>
                    E-mail
                </th>
                <th>
                    Telefone
                </th>
                <th>
                    Data de cadastro
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.Enterprises)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Email)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.PhoneNumber)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.CreatedAt)
                    </td>
                    <td>
                        <a asp-action="Edit" class="btn btn-primary btn-table" asp-route-id="@item.Id">Editar <i class="fa fa-pencil"></i></a>
                        <a asp-action="Details" class="btn btn-secondary btn-table" asp-route-id="@item.Id">Detalhes <i class="fa fa-book"></i></a>
                        <a asp-action="Delete" class="btn btn-danger btn-table" asp-route-id="@item.Id">Excluir <i class="fa fa-trash"></i></a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</section>

<section class="activity-history">
    <partial model="Model.ActivityHistory" name="../ActivityHistory/_ActivityHistory.cshtml" />
</section>